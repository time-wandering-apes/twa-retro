{
  "scripts": {
    "burn": "del-cli --force dist && tsc && node dist/burn.js",
    "top": "del-cli --force dist && tsc && node dist/top-burn.js",
    "fix-meta": "del-cli --force dist && tsc && node dist/fix-meta.js",
    "full-meta": "del-cli --force dist && tsc && node dist/full-meta.js"
  },
  "devDependencies": {
    "@types/lodash": "^4.14.194",
    "@types/node": "^20.2.1",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "dotenv": "^16.0.3",
    "lodash": "^4.17.21",
    "ton-core": "^0.49.1"
  }
}
