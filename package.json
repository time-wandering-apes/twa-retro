{
  "scripts": {
    "build": "del-cli --force dist && tsc",
    "burn": "del-cli --force dist && tsc && node dist/burn.js"
  },
  "devDependencies": {
    "@types/lodash": "^4.14.194",
    "@types/node": "^20.2.1",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "dotenv": "^16.0.3"
  }
}
